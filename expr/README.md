# expr/

Expression trees and their vectorized evaluation (predicates, arithmetic,
comparisons over column batches).

**WP-0 status:** empty skeleton — no headers, no logic yet. From-scratch:
no query-engine/dataframe libraries here.
