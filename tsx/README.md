# tsx/

Time-series execution support (windowing / time-bucketed operators over the `TS`
column type — int64 ns since epoch).

**WP-0 status:** empty skeleton — no headers, no logic yet. From-scratch:
no query-engine/dataframe libraries here.
