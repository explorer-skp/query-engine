# plan/

The dataframe / logical plan API (this engine has no SQL parser by design) and the
wiring that turns a plan into a tree of operators.

**WP-0 status:** empty skeleton — no headers, no logic yet. From-scratch:
no query-engine/dataframe libraries here.
